query GetCountryDetails($code:ID!) {
    country(code: $code) {
        code
        name
        native
        capital
        emoji
        currency
        phone
        continent {
            name
        }
    }
}